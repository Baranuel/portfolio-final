// schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = "postgres://s.baran.dev:ivlZ1Ic6BwUS@ep-blue-cell-08471803-pooler.eu-central-1.aws.neon.tech/neondb?pgbouncer=true&connect_timeout=10" // uses connection pooling
  directUrl = "postgres://s.baran.dev:ivlZ1Ic6BwUS@ep-blue-cell-08471803.eu-central-1.aws.neon.tech/neondb?connect_timeout=10" // uses a direct connection
}

model Project {
  id        String     @default(cuid()) @id
  title     String  
  category  String
  year     Int
  image     String     @default("")
  demo      String     @default("")
  technologies String[]
  objectives Objective[] 
}

model Objective {
  id String @default(cuid()) @id
  title String
  description String @db.VarChar(120)
  project Project @relation(fields: [projectId], references: [id])
  projectId String
}

model Endorsement {
  id String @default(cuid()) @id
  amount Int @default(0)
  icon EndorsementIcon
}


enum EndorsementIcon {
  Hearth
  Trash
  Inspire
}
